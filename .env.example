# Copy to .env.local to override. Not needed for `npm run dev`, which proxies /api to the local .NET backend.
# For a hosted frontend (Vercel), set this to the public URL of the API, including /api:
VITE_API_BASE=https://tender-tracker-api.onrender.com/api
